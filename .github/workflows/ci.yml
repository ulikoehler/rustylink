name: CI

# Run on pushes and pull requests to main (or master)
on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  build:
    name: Build & test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      # install Rust (ubuntu-latest ships rustup)
      - name: Install Rust
        run: rustup toolchain install stable --profile minimal --component clippy,rustfmt

      # optionally cache cargo registry and git index
      - uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/registry/index
            ~/.cargo/registry/cache
          key: ${{ runner.os }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-registry-

      - name: Check formatting
        run: cargo fmt -- --check

      - name: Run Clippy
        run: cargo clippy --all-features -- -D warnings

      - name: Cargo build
        run: cargo build --all-features

      - name: Run tests
        run: cargo test --all-features

  coverage:
    name: Coverage
    needs: build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Install Rust
        run: rustup toolchain install stable --profile minimal

      # cache the tarpaulin binary to avoid reinstalling every run
      # bump the vN suffix to force a newer tarpaulin version
      - name: Cache tarpaulin
        id: cache-tarpaulin
        uses: actions/cache@v5
        with:
          path: ~/.cargo/bin/cargo-tarpaulin
          key: ${{ runner.os }}-cargo-tarpaulin-v1

      - name: Install cargo-tarpaulin
        run: command -v cargo-tarpaulin >/dev/null || cargo install cargo-tarpaulin

      - name: Run tarpaulin
        # Xml output is compatible with codecov and many other tools
        run: cargo tarpaulin --all-features --out Xml

      # archive the report as an artifact so it can be inspected
      - uses: actions/upload-artifact@v7
        with:
          name: coverage-report
          # tarpaulin's Xml output is written as cobertura.xml
          path: cobertura.xml
          if-no-files-found: error
